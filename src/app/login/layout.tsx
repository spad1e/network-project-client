import React from "react";

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return (
    <div>
      {/* Header, sidebar, or wrapper styles */}
      {children}
    </div>
  );